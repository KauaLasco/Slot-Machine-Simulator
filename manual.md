# Manual Técnico - Simulador de Caça-Níquel

## Estrutura do Código
- `validador_de_vitorias`: verifica linhas vencedoras.
- `giros_do_caca_niquel`: gera símbolos aleatórios.
- `print_caca_niquel`: exibe resultado em formato 3x3.
- `deposito`: coleta depósitos do jogador.
- `coleta_do_numero_de_linhas`: define número de linhas apostadas.
- `valor_da_aposta`: define valor da aposta por linha.
- `selecionar_opcao`: permite depositar ou refazer aposta.
- `giros`: mantém o loop das rodadas, validando saldo e apostas até que uma jogada seja realizada.
- `main`: executa e finaliza o software (loop principal do jogo).

## Fluxo
1. Depósito inicial
2. Escolha de linhas e aposta
3. Validação de saldo
4. Giro da máquina
5. Exibição de resultado
6. Atualização de saldo e loop das rodadas
7. Repetição ou saída

--- 

# 📘 Technical Manual – Slot Machine Simulator 

## 🧩 Code Structure
- `validador_de_vitorias`: checks winning lines.
- `giros_do_caca_niquel`: generates random symbols.
- `print_caca_niquel`: displays the result in a 3x3 format.
- `deposito`: collects player deposits.
- `coleta_do_numero_de_linhas`: defines the number of bet lines.
- `valor_da_aposta`: defines the bet amount per line.
- `selecionar_opcao`: allows the player to deposit more or redo the bet.
- `giros`: It keeps the rounds looping, validating balances and bets until a play is made.
- `main`: Executes and terminates the software (main game loop).

## 🔄 Flow 
1. Initial deposit
2. Choose lines and bet
3. Balance validation
4. Machine spin
5. Result display
6. Balance update and round loop
7. Repetition or exit
